#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run linting first to catch code quality issues
npm run lint

# Run unit tests only (integration tests require Things3 running — use `npm run test:all` manually)
npm run test:unit